[package]
name = "fizz_manual_comment_input"
version = "0.1.0"
description = "Fizz comment source — operator manual input (stdin lines) → normalized Comment NDJSON"

[dependencies]
fizz_protocol = { git = "https://github.com/aiviecast/fizz-protocol", tag = "v0.2.2" }
